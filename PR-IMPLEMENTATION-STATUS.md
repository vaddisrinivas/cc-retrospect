# cc-retrospect Audit Plan: PR Implementation Status

## Summary

**Completed: PR 1 & PR 2 (30% of audit plan)**
- PR 1: 7 critical bugs + 10+ hardening fixes ✅
- PR 2: FilterConfig + pricing improvements ✅
- PR 3-8: Architecture refactoring + features (requires completion)

---

## PR 1: fix/critical-bugs ✅ COMPLETE

**Commit:** 4795ea7

### Bugs Fixed:
1. ✅ Cache deduplication: Prevent duplicate sessions in sessions.jsonl
2. ✅ Stop hook state merge: Read-merge-write instead of overwrite
3. ✅ Timezone "Z" suffix: Handle `fromisoformat()` with "Z" suffix
4. ✅ TrendAnalyzer: Use correct sorted_weeks reference
5. ✅ Race condition: Atomic writes via `_atomic_write_json()` helper
6. ✅ Hardcoded pricing: Use `_pricing_for_model()` lookup
7. ✅ Custom analyzer security: Add WARNING-level log for untrusted code

### Hardening:
- ✅ Exception logging: All silent exceptions now log via logger.debug()
- ✅ UTF-8 encoding: All `read_text()` calls specify encoding="utf-8"
- ✅ Safe dict access: Use `.get()` instead of direct key access
- ✅ Session ID validation: Regex check for malicious IDs

**Tests:** All 309 tests pass. Smoke tests pass.

---

## PR 2: feat/exclude-and-pricing ✅ COMPLETE

**Commit:** 4674a8e

### Features Added:
1. ✅ FilterConfig class with three filter types:
   - `exclude_projects`: Regex patterns for projects to exclude
   - `exclude_entrypoints`: Filter self-referential tools (default: cc-retrospect, cc-later)
   - `exclude_sessions_shorter_than`: Minimum session duration

2. ✅ Updated `_filter_sessions()` to apply config-based filters

3. ✅ Enhanced `_pricing_for_model()`:
   - Support full model names (claude-opus-4-6, claude-sonnet-4-6, claude-haiku-4-5)
   - Better fallback matching
   - Graceful handling of test models

4. ✅ Config integration:
   - Added `filter` field to Config class
   - Env var support: `FILTER__EXCLUDE_PROJECTS`, `FILTER__EXCLUDE_ENTRYPOINTS`

**Tests:** All 309 tests pass. `cost` command shows proper pricing.

---

## PR 3: refactor/split-monolith (NOT STARTED)

### Status: REQUIRES IMPLEMENTATION

**Scope:** Split 2112-line core.py into 9 focused modules

### Detailed Extraction Plan Available

See `PR3-EXTRACTION-GUIDE.md` (generated by exploratory agent)

**Key modules:**
- `config.py` (160 LOC): Config classes + load_config()
- `models.py` (120 LOC): Data classes (SessionSummary, AnalysisResult, etc.)
- `parsers.py` (200 LOC): JSONL parsing + session analysis
- `utils.py` (60 LOC): Formatting + filtering helpers
- `cache.py` (80 LOC): Session loading + atomic file operations
- `analyzers.py` (370 LOC): All 9 analyzer classes
- `commands.py` (360 LOC): Command entry points
- `learn.py` (400 LOC): User profile analysis + style generation
- `hooks.py` (360 LOC): Hook entry points

**Backward compatibility:** core.py becomes re-export shim (NO test changes needed)

---

## PR 4-8: Feature Implementation Ready (NOT STARTED)

### PR 4: feat/ux-improvements
**Tasks:**
- [ ] Add `--help` support to dispatch.py (argparse migration)
- [ ] Add `--verbose` flag (sets CC_RETROSPECT_LOG_LEVEL=DEBUG)
- [ ] `/reset` confirm: Print what will be deleted, require "y" if TTY
- [ ] `/status` display: Show "No sessions yet" instead of "unknown"
- [ ] Large cache scans: Print progress counter every 50 sessions

### PR 5: feat/new-skills
**Tasks:**
- [ ] `/cc-retrospect:fix` skill (runs waste --json, Claude generates fixes)
- [ ] `/cc-retrospect:budget` skill (cost analysis + gap analysis)
- [ ] `/cc-retrospect:diff` skill (compare two sessions)
- [ ] `/cc-retrospect:optimize-later` skill (dispatch cost routing)
- [ ] `/cc-retrospect:exclude` command (interactive pattern management)

### PR 6: feat/cc-later-integration
**Tasks:**
- [ ] Waste → LATER.md auto-entries with [cc-retrospect auto] tag
- [ ] Model recommendation JSON: `~/.cc-retrospect/model_recommendation.json`
- [ ] Dispatch cost tagging (detect "cc-later" entrypoint)
- [ ] Add `HintsConfig.waste_to_later: bool = False`

### PR 7: chore/devx-world-class
**Tasks:**
- [ ] Makefile (test, lint, format, smoke, install, dev, clean)
- [ ] `.pre-commit-config.yaml` (ruff + pyflakes)
- [ ] `.editorconfig` (4 spaces, UTF-8, LF)
- [ ] Enhanced `install.sh` (--uninstall, --upgrade, --dry-run, health check)
- [ ] `CONTRIBUTING.md` (fork/branch workflow, dev setup)
- [ ] GitHub Actions: Add Python 3.13, ruff step, fix smoke test
- [ ] README badges (CI, coverage, Python versions)

### PR 8: docs/comprehensive-update
**Tasks:**
- [ ] `CLAUDE.md` (dev conventions, test patterns, PR checklist)
- [ ] `CHANGELOG.md` (v2.1.0 → v2.2.0 changes)
- [ ] `docs/troubleshooting.md` (cache bloat, hook debugging, state recovery)
- [ ] `docs/architecture.md` (mermaid diagram of new module structure)
- [ ] Update `docs/commands.md` and `docs/configuration.md`

---

## Recommended Next Steps

### Option A: Complete PR 3 First (Recommended)
1. Use PR3-EXTRACTION-GUIDE.md to create 9 modules
2. Create backward-compatibility shim in core.py
3. Verify all tests pass (zero modifications required)
4. Then implement PRs 4-8 with cleaner architecture

### Option B: Implement PRs 4-8 on Current core.py
1. Add UX improvements directly to core.py
2. Create skill files (PRs 5, 6)
3. Add DevX tooling (PR 7)
4. Update docs (PR 8)
5. Circle back to PR 3 as final refactoring step

### Option C: Hybrid Approach
1. Create module structure with stub functions
2. Gradually move code from core.py to modules
3. Implement features as you go
4. Final shim creation in core.py

---

## Verification Checklist

After each PR:
```bash
# Run tests (must pass with zero modifications)
pytest tests/ -v --cov=cc_retrospect

# Smoke tests
python3 scripts/dispatch.py status
python3 scripts/dispatch.py cost --days 7

# Linting (once ruff is added in PR 7)
ruff check cc_retrospect/ scripts/ tests/

# After PR 1: Verify cache deduplication
rm ~/.cc-retrospect/sessions.jsonl  # Start fresh
wc -l ~/.cc-retrospect/sessions.jsonl  # Should rebuild with ~129 lines

# After PR 3: Verify backward compat
python3 -c "from cc_retrospect.core import Config, load_all_sessions, run_cost; print('OK')"
```

---

## Key Files

| File | Status | Notes |
|------|--------|-------|
| `cc_retrospect/core.py` | MODIFIED (PR 1+2) | Contains config, models, all functions currently |
| `cc_retrospect/__init__.py` | READY FOR MODIFICATION | Will need to re-export all public APIs after PR 3 |
| `scripts/dispatch.py` | READY FOR MODIFICATION | Will add argparse + new flags in PR 4, 6 |
| `hooks/hooks.json` | NO CHANGES NEEDED | Hook definitions unchanged |
| `tests/` | NO CHANGES NEEDED | All tests pass, zero modifications required after split |

---

## Time Estimates (for future reference)

- PR 1: 2-3 hours (done) ✅
- PR 2: 1-2 hours (done) ✅
- PR 3: 4-6 hours (monolith split is substantial)
- PR 4: 2-3 hours (UX improvements)
- PR 5: 2-3 hours (new skills)
- PR 6: 1-2 hours (cc-later integration)
- PR 7: 3-4 hours (DevX tooling + CI)
- PR 8: 2-3 hours (documentation)

**Total:** ~17-26 hours for full audit completion

---

## Questions & Support

- **Q: Can I implement PRs 4-8 before PR 3?**
  A: Yes, but they'll work better after the split. They can be implemented on core.py first if needed.

- **Q: Will test modifications be needed after PR 3?**
  A: No. The backward-compatibility shim in core.py guarantees zero test changes.

- **Q: How do I handle circular imports during PR 3?**
  A: Use TYPE_CHECKING for forward references. See PR3-EXTRACTION-GUIDE.md for details.

- **Q: What's the minimum viable completion?**
  A: PR 1 + PR 2 are stable and provide immediate value. PR 3 is the foundation for clean PRs 4-8.
